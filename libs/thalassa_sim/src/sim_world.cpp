#include "thalassa/sim/sim_world.hpp"

// SimWorld is header-only so far (see sim_world.hpp). This
// translation unit exists so thalassa_sim is a real compiled library from
// day one — TODO: systems (movement, combat, etc.) will add .cpp
// files here rather than the target needing to be restructured later.

namespace thalassa::sim {

// Intentionally empty

}  // namespace thalassa::sim
