# Thalassa

A deterministic, headless-first, high-entity-count RTS/MOBA simulation and
networking engine, designed to be reused inside Unreal Engine 5 as a pure
simulation backend (UE5 owns rendering only).

